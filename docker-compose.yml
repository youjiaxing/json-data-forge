version: '3.8'

services:
  app:
    build: .
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    # 从 .env 文件读取环境变量，或者直接在系统环境中设置
    environment:
      - API_KEY=${API_KEY}
